/** @type {import('next').NextConfig} */
const nextConfig = {images: {
    domains: ['media.tenor.com'], // Add the hostname(s) of the external image source(s) here
  },
};

module.exports = nextConfig

    
  